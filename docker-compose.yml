services:
  api:
    image: ${IMAGE:-mail-service}
    restart: unless-stopped
    network_mode: "host"
    env_file:
      - .env
    command: >
      sh -c "node src/database/migrate.js && node src/index.js"

  worker:
    image: ${IMAGE:-mail-service}
    restart: unless-stopped
    network_mode: "host"
    env_file:
      - .env
    command: ["node", "src/workers/emailWorker.js"]

  bounce-handler:
    image: ${IMAGE:-mail-service}
    restart: unless-stopped
    network_mode: "host"
    env_file:
      - .env
    command: ["node", "src/workers/bounceHandler.js"]
